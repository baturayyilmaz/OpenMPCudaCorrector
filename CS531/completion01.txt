 double avgArrPar_02(int arr[]){$<n>$int nthreads = 16;$<n>$double total = 0;$<n>$int PADDING = 8;$<n>$double s_priv[nthreads * PADDING] = {0};$<n>$$<n>$#pragma omp parallel num_threads(nthreads)$<n>${$<n>$int t = omp_get_thread_num();$<n>$#pragma omp for$<n>$for (int i = 0; i < ARRAY_SIZE; i++){$<n>$s_priv[t * PADDING] += arr[i] / ARRAY_SIZE;$<n>$}$<n>$}$<n>$$<n>$for (int i = 0; i < nthreads; i++)$<n>${$<n>$total += s_priv[i * PADDING];$<n>$}$<n>$$<n>$return total;$<n>$$<n>$